{
  "name": "@foam/agent-loop",
  "version": "0.1.0",
  "private": true,
  "description": "Implement and review loop: a coder and a reviewer session alternate over a spec until the review is clean",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "start": "node src/cli.ts",
    "build": "tsc --noEmit -p tsconfig.json",
    "test": "yarn test:unit",
    "test:unit": "vitest run",
    "bench": "echo 'No benchmarks for @foam/agent-loop'",
    "lint": "oxlint src"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.214",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "oxlint": "^1.57.0",
    "typescript": "^5.8.0",
    "vitest": "^3.2.6"
  }
}
